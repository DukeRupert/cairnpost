package pages

import "github.com/dukerupert/cairnpost/web/templates/layouts"

templ Deals() {
	@layouts.Base("Deals", "/deals") {
		<div class="max-w-4xl">
			<h1 class="text-2xl font-semibold text-gray-900">Deals</h1>
			<p class="mt-2 text-gray-500">Deal list will appear here.</p>
		</div>
	}
}
