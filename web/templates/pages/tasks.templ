package pages

import "github.com/dukerupert/cairnpost/web/templates/layouts"

templ Tasks() {
	@layouts.Base("Tasks", "/tasks") {
		<div class="max-w-4xl">
			<h1 class="text-2xl font-semibold text-gray-900">Tasks</h1>
			<p class="mt-2 text-gray-500">Task list will appear here.</p>
		</div>
	}
}
